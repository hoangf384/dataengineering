# những lệnh đã gõ trên shell

## Tạo thư mục
```shell
cd ~ && mkdir Dataengineering
```

```shell
touch test.py
echo 'print("hello world")' > ./test.py
python *.py
```

```shell
mkdir -p ./doc && touch README.md
```

```shell
cd ~/Dataengineering
git init
```

```shell
git add .
git commit -m "initial commit"
git branch -m main
```


> nhớ set up ssh
```shell
git remote add origin git@github.com:username/reponame.git
```

```shell
git push -u origin main
```

> post first commit, first push

```bash
cd .. && touch .gitignore
```
